## Java Swing Calculator

A desktop calculator built using Java Swing, featuring a clean UI and full functionality for basic arithmetic operations. It mimics a simple handheld calculator with support for addition, subtraction, multiplication, division, decimal numbers, square roots, clear (AC), and delete (DEL) functionality.

## Features

Simple and intuitive GUI using Java Swing.

Supports basic arithmetic operations: +, -, ×, ÷.

Supports decimal numbers.

Square root operation with error handling for negative numbers.

AC (All Clear) button to reset the calculator.

DEL (Delete) button to remove the last digit or operator.

Dynamic display: numbers and operators are right-aligned, and the font size adjusts automatically to fit the screen.

Custom colors for numbers, operators, and special buttons.

## How to Use

You can use this calculator by downloading the .exe file for Windows:

Download the .exe file from the Releases section
 (or wherever you host it).

Double-click the .exe to launch the calculator.

Use the buttons to perform calculations.

Click number buttons to enter digits.

Click an operator (+, -, ×, ÷) to perform operations.

Press = to get the result.

Use AC to clear everything.

Use DEL to delete the last digit/operator.

Use √ to calculate the square root of the current number.

## Technologies Used

Java SE 8+

Swing (JFrame, JPanel, JButton, JLabel)

GridLayout and BorderLayout for UI design

Custom color styling

## Future Enhancements

Support for parentheses and complex expressions.

Scientific calculator functions (sin, cos, log, etc.).

Keyboard input support.

Cross-platform version for macOS and Linux.


## Getting Started

Welcome to the VS Code Java world. Here is a guideline to help you get started to write Java code in Visual Studio Code.

## Folder Structure

The workspace contains two folders by default, where:

- `src`: the folder to maintain sources
- `lib`: the folder to maintain dependencies

Meanwhile, the compiled output files will be generated in the `bin` folder by default.

> If you want to customize the folder structure, open `.vscode/settings.json` and update the related settings there.

## Dependency Management

The `JAVA PROJECTS` view allows you to manage your dependencies. More details can be found [here](https://github.com/microsoft/vscode-java-dependency#manage-dependencies).


